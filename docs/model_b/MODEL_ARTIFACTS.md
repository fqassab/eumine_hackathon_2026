# Model artifacts

The trained ProphX_v2_scaled model binaries are stored separately because GitHub
does not permit new Git LFS objects to be uploaded to this public fork.

Required files:

- `physics_model_ef.joblib`
- `physics_model_bg.joblib`
- `feature_columns.joblib`
- `metadata.joblib`
- `discrepancy_lookup.joblib`

Place all five files in:

```text
models/prophx_v2_scaled/